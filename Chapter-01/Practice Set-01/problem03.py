# Install an external module and use it to perform an operation of your interest.
import datetime 
date_today = datetime.date.today() 
time_now = datetime.datetime.now().time() 
print(date_today) 
print(time_now) 

# Problem-03 is Completed